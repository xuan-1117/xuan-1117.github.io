---
title: "取证"
date: 2026-03-21 22:32:13
categories: "应急总结"
tags:
  - "应急总结"
cover: /img/backgrounds/bg-11.png
top_img: /img/backgrounds/bg-11.png
disableNunjucks: true
---

# WINDOWS取证
## <font style="color:rgb(31, 35, 40);">Windows 事件日志 （.evtx）</font>
**<font style="color:rgb(31, 35, 40);">关键事件编号：</font>**

| **<font style="color:rgb(31, 35, 40);">事件ID</font>** | **<font style="color:rgb(31, 35, 40);">描述</font>** |
| --- | --- |
| <font style="color:rgb(31, 35, 40);">1001</font> | <font style="color:rgb(31, 35, 40);">错误检查/重启</font> |
| <font style="color:rgb(31, 35, 40);">41</font> | <font style="color:rgb(31, 35, 40);">不洁关闭</font> |
| <font style="color:rgb(31, 35, 40);">4720</font> | <font style="color:rgb(31, 35, 40);">创建的用户账户</font> |
| <font style="color:rgb(31, 35, 40);">4722</font> | <font style="color:rgb(31, 35, 40);">启用用户账户</font> |
| <font style="color:rgb(31, 35, 40);">4724</font> | <font style="color:rgb(31, 35, 40);">尝试重设密码</font> |
| <font style="color:rgb(31, 35, 40);">4726</font> | <font style="color:rgb(31, 35, 40);">用户账户已删除</font> |
| <font style="color:rgb(31, 35, 40);">4738</font> | <font style="color:rgb(31, 35, 40);">用户账户更改</font> |
| <font style="color:rgb(31, 35, 40);">4781</font> | <font style="color:rgb(31, 35, 40);">账户名称更改（更名）</font> |


**<font style="color:rgb(31, 35, 40);">用 python-evtx 解析：</font>**

```plain
import Evtx.Evtx as evtx
import xml.etree.ElementTree as ET

with evtx.Evtx("Security.evtx") as log:
    for record in log.records():
        xml_str = record.xml()
        root = ET.fromstring(xml_str)
        ns = {'ns': 'http://schemas.microsoft.com/win/2004/08/events/event'}

        event_id = root.find('.//ns:EventID', ns).text
        if event_id == '4720':
            data = {}
            for d in root.findall('.//ns:Data', ns):
                data[d.get('Name')] = d.text
            print(f"User created: {data.get('TargetUserName')}")
```

## <font style="color:rgb(31, 35, 40);">注册分析</font>
```plain
# RegRipper
rip.pl -r NTUSER.DAT -p all

# Key hives
NTUSER.DAT   # User settings
SAM          # User accounts
SYSTEM       # System config
SOFTWARE     # Installed software
```

### <font style="color:rgb(31, 35, 40);">OEMInformation 后门检测</font>
```plain
from Registry import Registry

reg = Registry.Registry("SOFTWARE")
key = reg.open("Microsoft\\Windows\\CurrentVersion\\OEMInformation")
for val in key.values():
    print(f"{val.name()}: {val.value()}")
```

## <font style="color:rgb(31, 35, 40);">SAM 数据库分析</font>
**<font style="color:rgb(31, 35, 40);">必备文件：</font>**

+ `<font style="color:rgb(31, 35, 40);background-color:rgba(129, 139, 152, 0.12);">Windows/System32/config/SAM</font>`<font style="color:rgb(31, 35, 40);">- 密码哈希</font>
+ `<font style="color:rgb(31, 35, 40);background-color:rgba(129, 139, 152, 0.12);">Windows/System32/config/SYSTEM</font>`<font style="color:rgb(31, 35, 40);">- 启动密钥</font>

```plain
from impacket.examples.secretsdump import LocalOperations, SAMHashes

localOps = LocalOperations('SYSTEM')
bootKey = localOps.getBootKey()
sam = SAMHashes('SAM', bootKey)
sam.dump()  # username:RID:LM:NTLM:::
```

**<font style="color:rgb(31, 35, 40);">验证/破解NTLM：</font>**

```plain
from Crypto.Hash import MD4

def ntlm_hash(password):
    h = MD4.new()
    h.update(password.encode('utf-16-le'))
    return h.hexdigest()

# Crack with hashcat
# hashcat -m 1000 hashes.txt wordlist.txt
```

**<font style="color:rgb(31, 35, 40);">常见的RIDs：</font>**

+ <font style="color:rgb(31, 35, 40);">500 = 管理员</font>
+ <font style="color:rgb(31, 35, 40);">501 = 客人</font>
+ <font style="color:rgb(31, 35, 40);">1000+ = 用户账户</font>

## <font style="color:rgb(31, 35, 40);">主机文件隐藏数据</font>
地点： Windows/System32/drivers/etc/hosts

<font style="color:rgb(31, 35, 40);">攻击者会用过多空白隐藏数据：</font>

```plain
# Detect hidden content
xxd hosts | tail -20
```

## <font style="color:rgb(31, 35, 40);">联系人文件（.contact）</font>
地点： Users/<user>/Contacts/*.contact

**<font style="color:rgb(31, 35, 40);">笔记中的隐藏数据：</font>**

```plain
<c:Notes>h1dden_c0ntr4ct5</c:Notes>
```

## <font style="color:rgb(31, 35, 40);">RDP 会话事件 ID</font>
**<font style="color:rgb(31, 35, 40);">TerminalServices-LocalSessionManager\Operational：</font>**

| **<font style="color:rgb(31, 35, 40);">事件ID</font>** | **<font style="color:rgb(31, 35, 40);">描述</font>** |
| --- | --- |
| <font style="color:rgb(31, 35, 40);">21</font> | <font style="color:rgb(31, 35, 40);">会话登录成功</font> |
| <font style="color:rgb(31, 35, 40);">22</font> | <font style="color:rgb(31, 35, 40);">收到壳启动通知</font> |
| <font style="color:rgb(31, 35, 40);">23</font> | <font style="color:rgb(31, 35, 40);">会话注销成功</font> |
| <font style="color:rgb(31, 35, 40);">24</font> | <font style="color:rgb(31, 35, 40);">会话断开连接</font> |
| <font style="color:rgb(31, 35, 40);">25</font> | <font style="color:rgb(31, 35, 40);">会话重连成功</font> |
| <font style="color:rgb(31, 35, 40);">40</font> | <font style="color:rgb(31, 35, 40);">会话创建</font> |
| <font style="color:rgb(31, 35, 40);">41</font> | <font style="color:rgb(31, 35, 40);">会话开始（用户通知）</font> |
| <font style="color:rgb(31, 35, 40);">42</font> | <font style="color:rgb(31, 35, 40);">Shell 启动（用户通知）</font> |


**<font style="color:rgb(31, 35, 40);">TerminalServices-RemoteConnectionManager\Operational：</font>**

| **<font style="color:rgb(31, 35, 40);">事件ID</font>** | **<font style="color:rgb(31, 35, 40);">描述</font>** |
| --- | --- |
| <font style="color:rgb(31, 35, 40);">261</font> | <font style="color:rgb(31, 35, 40);">听众接收连接</font> |
| <font style="color:rgb(31, 35, 40);">1149</font> | <font style="color:rgb(31, 35, 40);">RDP用户认证成功（包含源IP）</font> |


**<font style="color:rgb(31, 35, 40);">RemoteDesktopServices-RdpCoreTS\Operational：</font>**

| **<font style="color:rgb(31, 35, 40);">事件ID</font>** | **<font style="color:rgb(31, 35, 40);">描述</font>** |
| --- | --- |
| <font style="color:rgb(31, 35, 40);">131</font> | <font style="color:rgb(31, 35, 40);">连接已接受（TCP，包含ClientIP：port）</font> |
| <font style="color:rgb(31, 35, 40);">102</font> | <font style="color:rgb(31, 35, 40);">客户端连接</font> |
| <font style="color:rgb(31, 35, 40);">103</font> | <font style="color:rgb(31, 35, 40);">断线（检查ReasonCode）</font> |


# <font style="color:rgb(31, 35, 40);">网络</font>
## <font style="color:rgb(31, 35, 40);">tcpdump 快速引用</font>
<font style="color:rgb(31, 35, 40);">命令行数据包捕获工具，用于快速网络取证分流。</font>

```plain
# Basic capture on interface
sudo tcpdump -i eth0

# Capture to file
sudo tcpdump -i eth0 -w capture.pcap

# Filter by source IP
sudo tcpdump -i eth0 src 192.168.1.100

# Filter by destination port
sudo tcpdump -i eth0 dst port 80

# Combined filter with file output
sudo tcpdump -i eth0 -w packets.pcap 'src 172.22.206.250 and port 443'

# Read from file with verbose output
tcpdump -r capture.pcap -v

# Show packet contents in ASCII
tcpdump -r capture.pcap -A

# Show hex + ASCII dump
tcpdump -r capture.pcap -X

# Count total packets
tcpdump -r capture.pcap -q | wc -l
```

**<font style="color:rgb(31, 35, 40);">常见过滤器：</font>**

| **<font style="color:rgb(31, 35, 40);">滤波器</font>** | **<font style="color:rgb(31, 35, 40);">描述</font>** |
| --- | --- |
| `host 10.0.0.1` | <font style="color:rgb(31, 35, 40);">IP的流量</font> |
| `net 192.168.1.0/24` | <font style="color:rgb(31, 35, 40);">整个子网</font> |
| `port 80` | <font style="color:rgb(31, 35, 40);">HTTP 流量</font> |
| `tcp` / `udp`/ `icmp` | <font style="color:rgb(31, 35, 40);">协议过滤器</font> |
| `src host X and dst port Y` | <font style="color:rgb(31, 35, 40);">综合</font> |


```plain
strings grep tcpdump -r capture.pcap -A | grep -i flag
```

## <font style="color:rgb(31, 35, 40);">从PCAP提取USB音频</font>
```plain
# Export ISO data with tshark
tshark -r capture.pcap -T fields -e usb.iso.data > audio_data.txt

# Convert to raw audio and import into Audacity
# Settings: signed 16-bit PCM, mono, appropriate sample rate
# Listen for spoken flag characters
```

